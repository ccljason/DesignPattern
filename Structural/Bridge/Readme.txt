
- Common Usage:
	- User Interface
	- Persistence
		- database persistence, file system persistence, streaming over the network persistence
	- .Net Provider Model
		- authorization provider, membership provider

- When you create a large class hierarchy, and you have to split those classes based on different 
	implementations as well as different abstractions, that's a good time to use the Bridge 
	Pattern.

